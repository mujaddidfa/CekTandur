/* const express = require('express');
const { getSolutions } = require('../controllers/solutionController');
const router = express.Router();

router.get('/solutions', getSolutions);

module.exports = router;
  */